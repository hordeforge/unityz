# Handover - Post-#75 sweep and shipping state

**Date:** 2026-09-01  
**Session:** codex-20260901-post75  
**Owner / next owner:** next agent session  
**Related:**

Clean-room UnityPy rewrite plan (PRs #47-#75)  
https://github.com/hordeforge/unityz/pull/75

## Current state

origin/main carries 29 consecutive PRs (#47-#75) shipped in this run.
The whole CLI surface is regression-swept and cross-validated against
UnityPy. The working tree also carries ANOTHER session's uncommitted
WIP (see Blockers).

### Shipped this run (PR #47-#75)

- edit: base64 byte-array patching (leaf + recursive subtree coercion),
  literal bracket-named fields (m_MeshMetrics[0]), extract --json
  round-trips byte-exactly
- edit --patch: raw-node sidecar byte patches ({"CAB-..resS":
  {"offset":N,"bytes":"<b64>"}})
- verify: streamed-reference integrity (m_StreamData/m_Resource must
  resolve); edit --verify passes sidecars (fixed the regression)
- bundle rebuild: keeps compression (pure-Zig LZ4 block compressor
  honoring the end-of-block conditions; LZMA/LZHAM sources convert
  losslessly; stale doc comment fixed)
- webfile rebuild: keeps gzip wrapping; gzip round-trip fuzz
- container: bare v4 serialized files reachable from the CLI (minimal
  v4 fixture); legacy UnityWeb/UnityRaw v2-5 bundles parse through
  bundle.parse (big-endian header + 13-byte UnityWeb LZMA framing,
  cross-validated against UnityPy)
- diff: reports raw container-node changes (sidecar edits)
- tests: fromFlatNodes unit tests; 11 fuzz campaigns (lz4, bundle,
  webfile, serialized, typetree, shader blob, fsb5, texture decode,
  image encoders, serialized_writer rewrite, legacy parse)
- docs: sweeps recorded; UnityPy parity sweep (3/3 textures, 8/9
  sprites byte-identical; one cosmetic alpha=0 edge difference)

Suite: 356/356 tests, fmt clean, docs-check clean except 3
pre-existing AGENTS.md flags + the other session's README section.

## Evidence

- zig build test --summary all: 356/356
- Full CLI sweep across all 5 samples: all pass (skin's non-zero exit
  is the designed contract)
- UnityPy cross-validation: bundles rebuild LZ4-readable, webfile gzip
  readable (UnityPy rejects gzip webfiles entirely - its gap), legacy
  UnityRaw/UnityWeb 73/73 objects, textures/sprites pixel parity
- doctor: all ok

## Next action

The next substantive slice is sample-dependent: TerrainData (class 27)
or Font (117/128) export (needs real samples), UnityArchive support
(not in UnityPy either), or legacy v6 bundles (UnityFS-style layout
per UnityPy's read_fs). Fetch samples online per the standing user
directive ("get them online or whatever you need").

## Blockers and risks

- ANOTHER session's uncommitted WIP sits in the working tree (still
  uncommitted since ~12:10 local): README (--trees docs, section with
  a docs-check flag), src/main.zig (~20 regions: parseInjectedTrees,
  injectedTreeFor, extract/sprite/audio changes), src/object_reader.zig
  (GizmoBox temp test). Do NOT overwrite; a stash backup exists
  (stash@{0}). Its session owns committing it.
- The README:95 docs-check flag is that session's section.
